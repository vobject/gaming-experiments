
--function f() print("asdf") end

-- function f (x, y)
    -- return (x^2 * math.sin(y)) / (1 - x)
-- end

function on_started()
  print("on_started() from Lua!!!")
end

-- print("Hello from Lua!")
